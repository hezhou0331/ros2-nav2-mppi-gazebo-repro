# M1 与 L2 SDK 使用说明核查

2026-09-12 已读取用户补充的 Word 原文、14 页渲染和 18 张内嵌图片。原件存档：[M1_L2_SDK_guide.docx](../vendor/manuals/M1_L2_SDK_guide.docx)，SHA-256 为 `ff439a5f700102b7b79b277cdd89fe604ef3028c0197c8dd50937482a29b6b05`；来源记录见 [核查元数据](evidence/sdk_manual_review.json)。原文件未修改。

这份资料介绍高层控制 SDK 的下载、版本匹配、网络、编译、控制示例与连接故障。它补充控制接口使用规则，但没有雷达点云 API、时钟同步设置、安装外参或狗内主机 SSH 账号。

| 原文位置（本次渲染页） | 文档内容 | 在本项目中的处理 |
| --- | --- | --- |
| 第 1 节，第 2–5 页 | genisom_robot_sdk；以 0.0.4 为示例，并称 0.1.1 为最新版；V4 配合相应固件 | 作为历史版本资料。截图兼容表只显示至 SDK 0.1.0，不能拿“最新版”或旧矩阵证明当前 0.2.1/固件 0.2.4 全兼容 |
| 第 2–3 节，第 6–8 页 | Ubuntu 22.04+、GCC 11+、CMake 3.8+、Boost 1.74+，编译 example | Orin 平台符合该说明的基本系统类型；保留现有独立导航依赖，不执行示例控制程序 |
| 第 4 节，第 8–9 页；FAQ Q3，第 13 页 | 高层 SDK 0.1.0 及以前用 8081，0.1.1 用 8082 | 高层端口必须随 SDK/固件核对，不能把所有版本都固定成一个端口；与 8083 低层、7447 传感器服务分开记录 |
| 第 4 节，第 9–12 页 | example_control、z 触发站立，其他按键触发动作 | 属于真实控制操作，不是只读连通测试；未运行 |
| FAQ Q1，第 12 页 | Wi-Fi 192.168.234.1；有线 192.168.168.168；Wi-Fi 可能需添加 168 段路由 | 我们已经从 Orin eth1/192.168.168.10 直连，不添加其 Wi-Fi 路由、不更改本机地址为示例 .12 |
| FAQ Q2，第 13 页 | SDK 与遥控器不能同时控制，先连接者占用权限 | 纳入控制权互斥说明。截图只写 ShakeHand failed，单凭该错误不能排除版本/端口等其他原因 |

文档第 5 页设备信息截图显示固件 0.2.4、App 1.1.7，这是说明书内的示例设备画面，不是本任务对当前机器的实时读取。第 9 页新端口正文与旧 8081 示例截图并存，使用时优先看明确标注版本的正文，仍需匹配实际库。

本项目当前真实传感器走 `192.168.168.100:7447` → Zenoh → Orin ROS，未依赖本说明的控制示例。软件时间映射、SI 单位与 AIRY 原生解码见 [输入适配说明](../m1_preflight/airy_adapter/README.md)。这份新资料不能填补缺失的物理外参，也不能证明后雷达已经锁定 PTP/GPS。
